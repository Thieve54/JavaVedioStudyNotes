常用的DOS命令： （windows下的cmd，只支持英文）DOS(Disk Operating System)
  dir:列出当前目录下的文件以及文件夹  (该处的:不为DOS命令的一部分)
  md:创建目录
  rd:删除目录
  cd:进入指定的目录     （使用CD现需要进入该盘）
  cd..:退回到上一级目录
  cd\:退回到根目录       （返回到E盘或者C盘）
  del:删除文件
  exit:推出dos命令行
  
  面向过程：只注重于过程顺序
  面向对象：分解成一个个小的模块
    张三打篮球：
      人的对象，人的动作，运动的器械 
      实例化一个张三的对象，对象有一个打篮球的动作，器械是篮球
    能更好的在抽象层面分析问题，在碰到跨度大的任务时，可以不需要想C一样重新编写一套代码完成
 
 Java易于移植到不同的平台之中"Write once,Run anywhere"，是存粹的面向对象语言，（引用取代指针）
 Java两大核心机制(实习中被经常问及)
    java虚拟机（java Virtual machine）,JVM  （Java的运行并不是直接在windows or mac 下直接运行而是通过虚拟机来完成的）
    Garbage collection,GC （C语言通过手动编写代码回收，优点：在内存不使用的时候更加精准高效但是容易出错（人的错误）导致内存溢出）
    （java的语言回收虽然自动，但是可能回收不及时（效率）） 
调整JDK（开发工具包（包含JRE））的版本可以在变量环境里面改变（为了方便支持多个项目不同版本的方便使用）
 
注释：
  // 单行注释
  /* */ 多行注释
  /**
  *
  */ 文档注释

1 Byte(字节) = 8 bit（位） 
1KB = 1024Byte
1MG = 1024KB
GB
TB
path的设置就是为了给java一个地址去寻找需要的东西（这样有事可以省略路径的填写）

通过javac 把字节码文件（源文件）变成class文件
每次都需要运行javac 重新编译class文件才能完成修改
编写 =>编译=> 运行
%%
public class Hello
{
	public static void main(String[] arges) 
	{
		System.out.println("Hello, World!");
	}
}
Hello 就是标识符; 标识符不能以数字，关键字开头
//
1.常量：在程序运行期间不变的量。

常量的分类
  1.字符串常量：凡是用双引号括起来的的全是常量
  2.整数常量:100
  3.浮点常量:2.5
  4.字符常量：单引号中的单个字符'A'
  5.布尔常量：true,false.
  6.空常量：null 没有任何数据 --并不能直接打印
 ''之间必须要有一个字符，不能为空，但可以是空格常量
 
 2.变量：在程序运行期间变的量。
  数据类型 变量名称 //创建一个变量
  varible = 数据值； =是赋值
 
 // 基本数据类型
          整数型  byte（-128to127不可溢出） short int long 
          浮点型  float(1.4e-45to3.4e38) double(比float更精确4.9e-325to1.7977e308) 
          字符型  char
          布尔类型 boolean 只需要一个字节 
     引用类型
          字符串，数组，类，接口，lambda
     转换字符类型可以加入后缀L or needed type 
     (100L) 100本应该是int 但我们需要long
     而且在字符超出范围的时候也可以使用L符号来规定显示
//
作用域：从定义变量的一开始，一直到所属的大括号为止。{}大括号所定义的范围
//
强制类型转换 （可能造成数据损失或精度损失）
int num =(int) 100L;强制转换成（）内int的类型
如果太大了，进行强制转换只能达到天花板的数字
//
byte/short/char 可以进行符号+ - 运算但是会被转换成int (ASC 代码）来计算 如'A' =65

 
  
